extends layout/default.pug

block meta
  title Widget

block content
  div(class="flex flex-col gap-4")
    textarea(id="texts", class="rounded w-full p-2 bg-light border-white outline-sky-600 outline-2 focus:outline focus:bg-dark" type="text" placeholder="One text per line")
    button(id="updateButton", class="block bg-sky-600 p-2 rounded hover:bg-sky-400") Update

block scripts
  script(src="./js/widget.ts" type="module")
